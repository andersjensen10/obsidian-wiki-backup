---
tags: [benchmarking, quality-control, pass, spark-infra]
status: pass
run_id: 2026-09-16-qc-0630
suite_version: 1.0.0
projectId: spark-infra
verification_time: 2026-09-16 06:31:11 CEST
---

# 06:30 benchmark quality-control gate — PASS — 2026-09-16

## Decision

**PASS for benchmark-artifact quality control.** The 04:00 run is complete and verifier-passing. Promotion and semantic/coding-quality approval remain separate and are **not approved**.

## Required gates

- Same-day readiness report is complete for acquisition/readiness and records the immutable candidate revision `5bc3e238d916f48a861bac2f8a1990a0e9b7e98d`, manifest-backed model checksum, isolated profile, and successful visible-answer smoke test.
- Exactly one new raw JSON artifact exists for the run: `/home/aj/llm-benchmark-local/runs/2026-09-16/raw.json`. It contains 30 result groups and 70 samples; no second `raw*.json` artifact exists.
- Raw artifact declares `suite_version: 1.0.0`; the locked 10-case matrix is present at C1/C2/C4.
- All 70 samples are successful and have non-empty visible responses. No sample is missing visible TTFT or generation throughput.
- Re-ran:
  `python3 /home/aj/llm-benchmark-runner/verify_run.py /home/aj/llm-benchmark-local/runs/2026-09-16/raw.json --suite-version 1.0.0 --min-cases 10`
  Result: `verified: true`, `results: 30`, exit 0.
- Recomputed raw SHA-256: `12ecf9b5cf2090ce2f2c4650da45e7d47a004b62e4d3c129c0c82a7dd4399c96`; it exactly matches `/home/aj/llm-benchmark-local/runs/2026-09-16/raw.sha256` and `verifier.json`.
- Candidate provenance is revision/checksum-backed and retained on internal SSD. No unverified artifact was accepted; raw results were not edited.
- Production Qwen remained healthy: `/health` returned `{"status":"ok"}`, `/v1/models` remained `qwen3.8-27b-aggressive-q5`, and the live PID 3517121 command exactly matched the captured production command. Isolated port 8015 is no longer listening.

## Exact discrepancies and caveats

- Toshiba archival remains **BLOCKED**, not a benchmark-quality failure: repeated `ntfs3(sdb1): MFT: r=279cb, expect seq=1 instead of 0!` warnings require approved filesystem repair and a clean preflight before any write.
- Dashboard ingest remains **BLOCKED** because `/home/aj/.benchmark-dashboard.env` has no configured API URL. No dashboard record is claimed.
- The verifier establishes mechanical completeness/integrity only. Structured-output validity, coding-test execution, and broader semantic quality review were not measured by this locked run; do not rank or promote on this artifact alone.

## Dashboard/process handoff for Herm

- **Measured schema changes:** none. The existing v1.0 schema is populated with `suite_version`, case/concurrency, visible response, `visible_ttft_s`, `generation_tps`, latency, token usage, and verifier/hash provenance. Keep the raw artifact and dashboard record keyed to the exact run/checksum.
- **New caveats:** the run is verifier-pass but non-promoted; Toshiba and dashboard ingest are independent blockers. C4 is stress/queueing evidence, not a promotion gate.
- **Queueing findings:** generation throughput stays roughly flat near 61–65 tok/s, while queueing dominates under load: measured visible-TTFT p50/p95 is C1 0.151/0.313 s, C2 0.277/5.909 s, C4 2.414/13.173 s; latency p50/p95 is C1 2.630/6.046 s, C2 2.801/11.566 s, C4 4.759/18.849 s.
- **Required dashboard refinements:** show verifier/hash state, promotion state, archival state, and dashboard-ingest state independently; expose C1/C2/C4 queueing and p95 visible TTFT; label qualitative/coding evidence as unmeasured rather than inferring it from mechanical success.
- **Process adjustment:** preserve the locked v1.0 suite; require the same preflight → isolated run → verifier → hash read-back → production-health read-back chain. Keep Toshiba disabled until filesystem repair is approved and verified. Restore/configure the dashboard endpoint separately; do not retry with guessed credentials or synthesize an ingest record.

No raw benchmark results were edited.
