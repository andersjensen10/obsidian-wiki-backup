---
tags: [benchmarking, quality-control, blocked, spark-infra]
status: blocked
run_id: 2026-09-17-qc-0630
suite_version: 1.0.0
projectId: spark-infra
verification_time: 2026-09-17 06:30 CEST
---

# 06:30 benchmark quality-control gate — BLOCKED — 2026-09-17

## Decision

**BLOCKED.** No benchmark artifact was produced for the 2026-09-17 slot, so this run cannot be called complete, verified, or successful. No raw result was edited and no catch-up run was attempted.

## Required gates

- Same-day readiness report exists and is **BLOCKED**, not complete. The selected candidate acquisition is incomplete: `4,464,275,456` of `22,663,387,424` expected bytes in `Qwen3.6-35B-A3B-UD-Q4_K_M.gguf.partial`.
- Same-day benchmark report: **absent**. The 04:00 report records that no benchmark was launched.
- Same-day raw JSON: **absent**. `/home/aj/llm-benchmark-local/runs/2026-09-17/` does not exist; no raw artifact was produced.
- Same-day SHA-256 manifest: **absent**. There is no same-day raw artifact to hash or compare.
- Consequently, exactly one new raw artifact, suite v1.0.0 execution, all 10 cases at C1/C2/C4, non-empty visible responses, TTFT, and generation throughput are **unverified for 2026-09-17**.
- The locked suite metadata and runner tests were available in readiness (`pytest`: `3 passed in 0.00s`), but the locked run was not consumed because readiness was blocked.

## Verifier and integrity evidence

The required verifier was re-run against the latest prior immutable artifact only, not presented as today's result:

```text
python3 /home/aj/llm-benchmark-runner/verify_run.py /home/aj/llm-benchmark-local/runs/2026-09-16/raw.json --suite-version 1.0.0 --min-cases 10
verified: true; results: 30; exit 0
```

Its SHA-256 is `12ecf9b5cf2090ce2f2c4650da45e7d47a004b62e4d3c129c0c82a7dd4399c96`, matching `/home/aj/llm-benchmark-local/runs/2026-09-16/raw.sha256`. That prior artifact has 30 groups / 70 samples, 70 successful, 0 empty visible responses, and 0 missing TTFT. It has no usage objects, so no generation-throughput-with-usage check was applicable there.

## Production safety

- Production `/health`: `{"status":"ok"}`.
- Production `/v1/models`: `qwen3.8-27b-aggressive-q5`.
- Live production command remains the captured Qwen3.8 service on port 8014 with the existing model path and flags; no restart, swap, or configuration change occurred.
- Current readiness also records fresh isolated CUDA/MPS initialization failure: `MPS server is not ready to accept new MPS client requests`.
- Toshiba archival remains blocked by `ntfs3(sdb1): MFT: r=279cb, expect seq=1 instead of 0!`; no archival write was attempted.

## Exact discrepancies and blockers

1. The candidate acquisition is a partial `.partial` file and has not passed completion or checksum verification.
2. Fresh isolated runtime readiness cannot be verified because CUDA/MPS initialization is not ready.
3. No same-day benchmark report, raw JSON, or SHA-256 manifest exists; therefore all current-run quality gates are unverified.
4. Toshiba archival remains independently blocked by the NTFS/MFT warning.
5. Dashboard ingest was not attempted because `/home/aj/.benchmark-dashboard.env` has no configured API URL.

## Dashboard/process handoff for Herm

- **Measured schema changes:** none; no current-day benchmark payload exists. Preserve separate `status=blocked`, verifier, promotion/quality, archival, ingest, and provenance states. Do not synthesize a dashboard record from the prior run.
- **New caveats:** a prior verifier-pass artifact exists, but it is from 2026-09-16 and does not clear today's missing artifact, incomplete acquisition, or failed fresh MPS readiness. Dashboard trend/leaderboard data must remain unchanged for today's slot.
- **Queueing or quality findings:** no new measurements. Retain the prior finding that C4 is stress/queueing evidence and that mechanical verifier success is not semantic or coding-quality approval.
- **Required dashboard refinements:** show blocked slots with explicit missing-artifact and readiness reasons; distinguish `not_run` from `failed` and `verified`; keep prior last-known-good data visible without attributing it to today's date; expose acquisition/runtime/archive/ingest gates independently.
- **Proposed benchmark-process adjustment:** keep the one-slot, fail-closed rule. Require acquisition completion + checksum, fresh isolated MPS readiness, then exactly one run, verifier, SHA read-back, and production-health read-back. Do not resume partial acquisition, repair Toshiba, change credentials, or create a catch-up benchmark from this QC job.

No raw benchmark results were edited.
