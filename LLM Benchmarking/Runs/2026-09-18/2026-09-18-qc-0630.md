---
tags: [benchmarking, quality-control, blocked, spark-infra]
status: blocked
run_id: 2026-09-18-qc-0630
suite_version: 1.0.0
projectId: spark-infra
verification_time: 2026-09-18 06:32 CEST
---

# 06:30 benchmark quality-control gate — BLOCKED — 2026-09-18

## Decision

**BLOCKED.** The readiness gate is blocked, the benchmark was not launched, and no same-day raw benchmark artifact exists. This slot is not complete, verified, or successful. No raw results were edited.

## Required gates

- Same-day readiness report exists and is **BLOCKED**, not complete. The isolated Qwen3.6-35B-A3B profile loaded and answered its smoke request, but GPU headroom is not independently verifiable: `nvidia-smi` reports NVIDIA GB10 memory as `N/A`, and the isolated server log records `common_fit_params: failed to fit params to free device memory: n_gpu_layers already set by user to -2, abort`.
- Same-day benchmark report exists and explicitly says **BLOCKED — benchmark not launched**.
- Exactly one new raw artifact: **FAIL — zero**. `/home/aj/llm-benchmark-local/runs/2026-09-18/raw.json` is absent; the run directory contains only readiness-support artifacts and `benchmark-report.md`.
- Same-day raw SHA-256 manifest: **FAIL — absent**. `raw.sha256` and `verifier.json` are absent. The candidate acquisition manifest is not a raw-run manifest and cannot substitute for it.
- `suite_version: 1.0.0` and the locked 10-case matrix are available as readiness metadata, but execution of all 10 cases at C1/C2/C4 is **unverified** because no raw run exists.
- Non-empty visible responses, TTFT, and generation throughput where usage exists are **unverified** for this slot.
- No unverified or mutable benchmark artifact was accepted.

## Verifier and integrity evidence

Re-ran the required verifier against the expected same-day artifact:

```text
python3 /home/aj/llm-benchmark-runner/verify_run.py /home/aj/llm-benchmark-local/runs/2026-09-18/raw.json --suite-version 1.0.0 --min-cases 10
FAIL: missing artifact: /home/aj/llm-benchmark-local/runs/2026-09-18/raw.json
exit 1
```

SHA-256 comparison is **not applicable** because the raw artifact and same-day raw manifest are both absent. No checksum was invented or compared against the candidate model manifest.

## Production safety

- Live production `http://192.168.0.139:8014/health`: HTTP 200, `{"status":"ok"}`.
- Live production `/v1/models`: `qwen3.8-27b-aggressive-q5`.
- The live process remains the captured Qwen3.8 command on port 8014 with the existing model path and flags; no restart, swap, or configuration change occurred.

## Exact discrepancies and blockers

1. Readiness is `BLOCKED` by unverifiable GPU memory headroom and the isolated llama.cpp fit warning.
2. Benchmark execution was withheld; there are zero new raw artifacts rather than exactly one.
3. No same-day raw JSON, raw SHA-256 manifest, or verifier artifact exists.
4. Therefore C1/C2/C4 coverage, visible response completeness, TTFT, and usage-conditioned generation throughput cannot be verified.
5. Toshiba archival remains independently blocked by the unresolved NTFS MFT warning; no archival write was attempted.
6. Townhall was reachable for an initial GET earlier in this run and exposed the relevant Herm benchmark/dashboard post `1ed500ad-139b-4dab-b482-46b953e450a2`. At post time, the Townhall API at `http://192.168.0.148:5173/api/townhall` refused connections. The required finding reply POST and read-back both failed with connection refused; no Townhall write is claimed.

## Dashboard/process handoff for Herm

- **Measured schema changes:** none; no current-day benchmark payload exists. Preserve independent `status=blocked`, readiness, artifact, verifier, checksum, promotion, archival, ingest, and provenance states.
- **New caveats:** the isolated server can pass health/smoke while GPU backing/headroom remains unverified. A successful smoke response must not clear the readiness gate. Keep the prior last-known-good benchmark visible without attributing it to 2026-09-18.
- **Queueing or quality findings:** no new measurements. C4 queueing and the prior throughput/visible-TTFT findings remain historical only; do not extend their trend with this slot.
- **Required dashboard refinements:** show `not_run` separately from `failed` and `verified`; display the missing raw artifact and readiness blocker prominently; keep verifier/hash, runtime, archival, and ingest gates independently visible; prevent dashboard ingestion when no verified raw artifact exists.
- **Proposed benchmark-process adjustment:** retain the fail-closed chain: complete immutable acquisition and headroom verification → isolated readiness → exactly one raw run → verifier → raw SHA read-back → production-health/command read-back. Add an explicit preflight assertion that the run directory contains no raw artifact before launch and exactly one after launch. Do not create a catch-up run from this QC job.

No raw benchmark results were edited.
