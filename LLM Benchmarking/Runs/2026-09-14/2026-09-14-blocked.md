---
 tags: [benchmarking, blocked, spark-infra]
 status: blocked
 date: 2026-09-14
 suite_version: 1.0.0
 projectId: spark-infra
---

# Nightly benchmark — blocked — 2026-09-14

## Outcome

No benchmark was run. The one-night benchmark slot was preserved because the selected candidate and isolated server profile were not ready. No model download, production restart, model swap, or promotion was attempted.

## Selected candidate

- Repository: `Qwen/Qwen3-Coder-30B-A3B-Instruct`
- GGUF source: `AtomicChat/Qwen3-Coder-30B-A3B-GGUF`
- Selected quantization: `Q4_K_M` (research estimate: 18.6 GB)
- Selection evidence: [[../Research/2026-09-13]]

## Preflight evidence

- Toshiba library: present at `/media/aj/TOSHIBA EXT1/LLM Model Library`.
- Filesystem: NTFS mounted read-write; `df` reported 954,915,984 KiB available (52% free), above the 20% retention floor; directory access is writable.
- I/O monitoring: kernel log review since 2026-09-13 04:00 found prior NTFS dirty-volume warnings, but no matching `I/O error`, `buffer I/O`, `blk_update`, or device-reset lines. The prior dirty warnings remain an operational concern and require continued monitoring.
- Candidate readiness: Toshiba `models/` is empty (0 bytes); no selected-candidate model or manifest was found ready to serve.
- Isolated server profile: Toshiba `server-profiles/` contains no profile entries; no safe candidate port/profile was available.
- Production server: still running unchanged on `0.0.0.0:8014` as `qwen3.8-27b-aggressive-q5`, with `--parallel 2`; its command and model path were captured during preflight.
- Runner: `/home/aj/llm-benchmark-runner` exists with `.venv`; targeted runner metric tests passed (`3 passed`). The directory is not a Git checkout, so a runner commit could not be recorded. The runner's `suite.json` is v1.0.0, but the locked suite notes that literal fixtures are not frozen beside a suite release; this is not sufficient for a comparable full run.

## Required before next run

1. Acquire and checksum-verify the selected GGUF into Toshiba quarantine, then atomically promote it with a manifest.
2. Create and validate an isolated llama.cpp server profile/port without interrupting production.
3. Make the runner a versioned checkout and freeze literal fixtures for suite v1.0.0, or issue a new locked suite version.
4. Capture the candidate revision, quantization, server flags, llama.cpp commit, and environment before running the full C1/C2/C4 streaming matrix.
