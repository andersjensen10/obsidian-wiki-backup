---
tags: [benchmarking, quality-control, blocked, spark-infra]
status: blocked
date: 2026-09-14
verification_time: 2026-09-14 06:32:22 CEST
suite_version: 1.0.0
projectId: spark-infra
---

# 06:30 benchmark quality-control gate — BLOCKED — 2026-09-14

## Decision

**BLOCKED.** The night's benchmark cannot be called successful or comparable.

## Exact discrepancies

- Required count is exactly one new raw artifact; discovered count is **0** in both run locations:
  - `/media/aj/TOSHIBA EXT1/LLM Model Library/runs/` — 0 files
  - `/home/aj/llm-benchmark-local/runs/` — 0 files
- No same-day raw JSON exists, so `suite_version: 1.0.0` is not verified in a raw run.
- No raw artifact means the required 10 cases at C1/C2/C4 are unverified (expected 30 result entries).
- Non-empty visible responses are unverified.
- TTFT and generation throughput where usage exists are unverified.
- No SHA-256 manifest was found; artifact immutability and verification cannot be established.

## Verification command

Re-ran:

```text
python3 /home/aj/llm-benchmark-runner/verify_run.py '/media/aj/TOSHIBA EXT1/LLM Model Library/runs/2026-09-14/raw.json' --suite-version 1.0.0
```

Result: `FAIL: missing artifact: /media/aj/TOSHIBA EXT1/LLM Model Library/runs/2026-09-14/raw.json` (exit 1).

Also checked the fallback:

```text
python3 /home/aj/llm-benchmark-runner/verify_run.py '/home/aj/llm-benchmark-local/runs/2026-09-14/raw.json' --suite-version 1.0.0
```

Result: `FAIL: missing artifact: /home/aj/llm-benchmark-local/runs/2026-09-14/raw.json` (exit 1).

Because no artifact or manifest exists, no SHA-256 comparison is possible.

## Production Qwen safety

The production service remains unchanged and healthy:

- `GET http://127.0.0.1:8014/health` → HTTP 200, `{"status":"ok"}`.
- `/v1/models` reports `qwen3.8-27b-aggressive-q5`.
- Current production command remains the captured `llama-server` on `0.0.0.0:8014`, `--parallel 2`, using `/home/aj/llama-models/Qwen3.8-27B-Uncensored-HauhauCS-Aggressive-MTP-GGUF/Qwen3.8-27B-Uncensored-HauhauCS-Aggressive-Q5_K_P.gguf`, with the same speculative draft model and flags recorded in the prior readiness report.

No raw results were edited. No benchmark promotion or production-affecting action was attempted.
