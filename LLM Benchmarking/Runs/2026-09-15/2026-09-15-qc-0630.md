---
tags: [benchmarking, quality-control, blocked, spark-infra]
status: blocked
run_id: 2026-09-15-qc-0630
suite_version: 1.0.0
---

# 06:30 QC gate — 2026-09-15

Decision: **BLOCKED**

## Exact discrepancies

- Same-day readiness report `2026-09-15-readiness-blocked.md` and `readiness.json` are BLOCKED: the selected Qwen3-Coder candidate, revision/checksum-backed manifest, and dedicated isolated candidate profile are absent. No benchmark was scheduled or run.
- Same-day benchmark report `2026-09-15-0400-blocked.md` records the 04:00 slot as consumed and blocked. It reports a new Toshiba NTFS MFT warning at 04:00:38.
- Required new-artifact count is **0**, not exactly 1. Neither `/media/aj/TOSHIBA EXT1/LLM Model Library/runs/2026-09-15/` nor `/home/aj/llm-benchmark-local/runs/2026-09-15/` exists.
- No same-day raw JSON or SHA-256 manifest is present. The only discovered manifest is the prior `2026-09-14-e2e/SHA256SUMS`; it is not a new artifact for this run and was not used to pass this gate.
- Re-ran the verifier against both expected same-day `raw.json` paths with `--suite-version 1.0.0`; both returned exit 1: `FAIL: missing artifact`.
- Therefore suite v1.0.0, all 10 cases at C1/C2/C4 (30 result groups), non-empty visible responses, TTFT, and generation throughput where usage exists are unverified.
- No unverified or mutable artifact was accepted.

## Production protection

- `http://192.168.0.139:8014/health` returned HTTP 200 and `{"status":"ok"}`.
- Production process PID `3517121` remains the unchanged llama-server command for alias `qwen3.8-27b-aggressive-q5`, using the existing Qwen3.8-27B model and production flags. No production service or raw result was modified.

## Dashboard/process handoff for Herm

- Measured schema changes: none; no new benchmark record exists to ingest. Keep the locked suite at v1.0.0 and do not synthesize timing, quality, or leaderboard values.
- New caveats: readiness is blocked before execution; Toshiba has a newly recorded NTFS MFT warning. The candidate/profile absence is an upstream gate, not a benchmark quality result.
- Queueing/quality findings: no new measurements. Prior Townhall guidance remains applicable: C4 is stress-only, and only `status=complete` plus verifier pass may rank.
- Required dashboard refinement: retain/render this blocked night as explicit blocked data or blocker state, keep the wall gated against leaderboard updates, and show artifact absence/provenance rather than stale or synthetic metrics. Do not ingest a nonexistent run.
- Process adjustment: next cycle must start with fresh preflight; do not catch up or retry the consumed 04:00 slot. Require candidate revision, checksum manifest, and isolated profile before launch; fail closed on the Toshiba warning until the storage gate is cleared.

No raw benchmark results were edited.
