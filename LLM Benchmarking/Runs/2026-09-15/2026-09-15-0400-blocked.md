---
tags: [benchmarking, automation, reliability, spark-infra]
status: blocked
run_id: 2026-09-15-0400
suite_version: 1.0.0
---

# Nightly benchmark — 2026-09-15 04:00

Status: **BLOCKED**

## Exact gates

The required same-day readiness is not `COMPLETE`; the 02:30 readiness artifact is `BLOCKED`. The selected candidate, `Qwen/Qwen3-Coder-30B-A3B-Instruct` from `AtomicChat/Qwen3-Coder-30B-A3B-GGUF`, `qwen3-coder-30b-a3b-Q4_K_M.gguf`, is absent from the Toshiba model library and local fallback. No revision or checksum-backed manifest is available. The dedicated isolated candidate profile is absent; the only Toshiba profile found is the prior baseline E2E profile. The storage recheck also observed a new NTFS MFT warning at 04:00:38 (`ntfs3(sdb1): MFT: r=279cb, expect seq=1 instead of 0!`), so storage is not treated as cleanly healthy.

Per the runbook, these are non-retryable gates for this slot. No benchmark was started and no second candidate or catch-up run was attempted.

## Evidence and checks

- Same-day readiness: [`readiness.json`](readiness.json), checked 2026-09-15 02:30:42 +02:00, status `BLOCKED`.
- Toshiba: `/media/aj/TOSHIBA EXT1`, `/dev/sdb1`, NTFS3, read-write, approximately 910.68 GiB free (52%); model library `models/` and `manifests/` contain zero files.
- Candidate/profile: no candidate file, manifest, or checksum; no dedicated candidate profile. Existing `server-profiles/e2e-baseline-2026-09-14` is baseline-only and was not used.
- Runner: `PYTHONPATH=/home/aj/llm-benchmark-runner /home/aj/llm-benchmark-runner/.venv/bin/python -m pytest -q /home/aj/llm-benchmark-runner/tests` — `3 passed in 0.00s`.
- Locked suite: `/home/aj/llm-benchmark-runner/suite.json`, version `1.0.0`, 10 cases, C1/C2/C4.
- Production: `http://127.0.0.1:8014/health` returned `{"status":"ok"}`; PID `3517121` and production command remained unchanged. Production was not restarted, swapped, or loaded under benchmark traffic.
- llama.cpp build available: build `931`, commit `4df29be4f4c3673f428170fda944a5b19f743bb`.
- Kernel evidence: `journalctl -k --since '2026-09-15 00:00:00'` showed the new NTFS MFT warning above and no new literal `I/O error` line.

## Safety result

No model download, filesystem repair, force-mount, server launch, production change, benchmark request, raw benchmark JSON, or `verify_run.py` execution occurred. The nightly benchmark slot is recorded as blocked; the next cycle must begin with fresh preflight and must not catch up this run.
