# Axiom Engine — Current State

> Last updated: `<date>` by `<who>`. Source of truth for day-to-day dev
> detail is `<your in-repo skill / docs, if any>` and `[[NOTES]]`; this page
> is the Obsidian-facing overview + status snapshot — same role
> `Agentic Chatroom.md` plays for that project.

## What it is

*(One paragraph: what Axiom Engine is, for whom, and the core mechanic/value.
See `[[Agenti Chatroom long term]]`-style founding vision doc, once one
exists for this project, for the fuller pitch.)*

## Where it lives

- Path: `<repo path>`
- Git: `<local-only / remote, branch, author>`
- Stack: `<languages, frameworks, datastore>`

## Live ports / environment (verified `<date>`)

| Port / endpoint | What |
|---|---|
| `<port>` | `<service>` |

*(Delete this section if the project has no long-running local services.)*

## Backing infra

*(Any external services, hardware, or accounts this project depends on —
mirror `[[LAN notes]]`-style detail if it's substantial enough to warrant its
own page.)*

## Status

*(Current headline state — what's shipped, what's in flight, what's blocked
and why. Update this each time something material changes; this is the
first thing anyone — including a QA Manager or PM starting a cycle —
should read to get oriented.)*

## Verify gate (before calling anything "done")

*(The exact commands / scripts that must pass before a story is shippable —
this is what the Scrummaster's "Definition of done" field should point
coding agents at. Keep this section accurate; stories are only as
verifiable as this list is real.)*

## Known gotchas

*(Anything a coding agent or reviewer would otherwise have to rediscover the
hard way — infra quirks, footguns, "looks fixed but isn't" traps.)*

## Feature Development pipeline

Work flows through `Feature Development/`, same shape as every project in
the portfolio: raw ideas land in `Feature Requests`, get matured into
`Backlog`, and ship into `Published features/Sprint N - Week WW` once done.

- **Active backlog:** *(link once a sprint exists)*
- **Full blueprint:** [[improvements]] (`Feature Development/Feature
  Requests`) — the long-term architecture/UX blueprint for this project.
- **Next-level vision:** *(link the active Board vision doc, see [[GOAL]])*

## Related notes

- [[NOTES]] — running session log.
- [[improvements]] — full blueprint & phasing.
- [[GOAL]] — current north-star pointer.
